# Классы задач по внешнему источнику (Serpent 2.1.32)

Универсальный каталог: какой синтаксис нужен для каждого класса задач и что
сверять в выходах. Никаких приватных входов и эталонов здесь нет — только метод.
Правило: метод, заданный в постановке (источник, отклик, геометрия), не менять.

## Класс A. Доза от гамма-изотопа на водном фантоме

Человек = цилиндр, заполненный водой (размеры порядка человеческого тела),
на фиксированном расстоянии от точечного источника.

Ключевой синтаксис:

```serpent
mat iso -22.6 vol 3E-7
Ir-192 1
src Iso p
    sg -1 1
    sp -200 0 0
    srad 0 5
det HUMAN
    dm H2O
    dv 226194.671
    dr -200 void
set nps 200000 100
set declib "xsdata/sss_endfb7.dec"
set pdatadir "xsdata/photon_data"
```

Сверять: `_res.m`, `DET HUMAN` (значение + ошибка), `_gsrc.m tot` (мощность испускания).

## Класс B. Источник распада `sg` → табличный `sb`

Два изотопа: гамма-эмиттер и тяжёлый изотоп со спонтанным делением.
Сначала счёт с `sg`, спектр испускания — детектором со спец-откликом
на предопределённой сетке; затем второй счёт с табличным `sb` и нормировкой
из файла испускания. Гамма и нейтроны — отдельно.

Первый прогон (`sg`):

```serpent
src isotope n
    sg -1 1
    sp -200 0 0
    srad 0 5
ene e 4 scale44
det NRG dr -11 void de e
set declib "xsdata/jef3.dec"
set nfylib "xsdata/sss_endfb7.nfy"
```

Второй прогон (`sb`, спектр = `DET NRG`, делённый на ширины групп):

```serpent
src isotope n
    sp -200 0 0
    srad 0 5
    sb 15 1
        3.0E-03 0
        ... точки спектра ...
    sm curium
set srcrate 1.03089E+11
```

Сверять: `mat_*_tot`/`tot` в `_gsrc.m`/`_nsrc.m` первого прогона = `set srcrate`
второго; спектры `DET NRG` обоих прогонов совпадают.

Ловушки:
- Предопределённую сетку (`scale44`) нельзя писать в `de` напрямую — только через `ene`.
- Нейтронный `sg` работает с транспортным нуклидом (`Cm-250.03c` + `nfylib`);
  фотонному нужен decay-нуклид без суффикса (`Cm-250`, `Ir-192`, `Co-60`) + `declib`.
- Файл распада для JEFF-изотопов (`jef3.dec`, JEFF-3.3) не лежит на VTT и не
  заменяется молча на `sss_endfb7.dec`.

## Класс C. Сечения изотопов (вне Serpent)

Полные сечения выбранных изотопов в точках (например 10 эВ, 1 кэВ, 1 МэВ),
проверка по внешнему БД (JANIS). Serpent тут используется только как потребитель
библиотеки (`set acelib`, ZAID `.03c`); сама проверка — ручная.

## Класс D. Импульс и время жизни в отражателе

Моноэнергетический импульсный источник (все частицы рождаются в t=0),
пары отражателей (лёгкая/тяжёлая вода, сталь/смеси, свинец, графит чистый/грязный).
Среднее время жизни — детектором по временным бинам.

```serpent
src isotope n
    se 1
    sp -20 0 0
    srad 0 5
    sd 1.0 0 0
set srcrate 1E+06
tme TB 2 100 0 0.1
det LIFE
    dm D20
    di TB
therm grap gre7.00t
mat gr sum moder grap 6000
6000.03c 0.08363
```

Грязный графит: примеси бора (`5010.03c`, `5011.03c` порядка 1E-7).
Сверять: `DET LIFE` по временным бинам, сравнение пар отражателей.

## Класс E. Пластина + неаналоговое моделирование

Между источником (класс A) и фантомом — пластина. Толщина подбирается так,
чтобы статошибка дозы выросла до ~30%; затем ошибка давится картами важности.

```serpent
wwgen GENA 1E-12 4 3 -1 1
    -210 35 30
    -45 45 30
    -110 110 30
wwin WIN
    wi 1 5
        GENA 1
        GENA 1
        GENA 1
        GENA 1
        GENA 1
```

Сверять: ошибка `DET HUMAN` до/после, файлы `.wwd*`.

## Класс F. Подкритическая сборка, поток vs расстояние

Решётка топливных блоков в замедлителе, внешний источник со средней энергией
(например Pu-Be, 4.84 МэВ — табличным `sb`), детекторы в экспериментальных каналах.

```serpent
pin cyl_block
    Fuel 1.5
    Air 1.8
    Aluminium 2
    Graphite
```

Сверять: `_res.m`, `_det0.m` по позициям детекторов, зависимость потока от расстояния.

## Данные для всех классов (относительные пути)

```serpent
set acelib "xsdata/data.xsdata"
set declib "xsdata/jef3.dec"
set nfylib "xsdata/sss_endfb7.nfy"
set pdatadir "xsdata/photon_data"
```

`declib` — по задаче (`jef3.dec` для JEFF-распадов, `sss_endfb7.dec` для остальных);
`nfylib` — нейтронам; `pdatadir` — фотонам. Раскладка и проверка — `tools/setup-env.sh`,
быстрая проверка — `sh tools/doctor.sh`. CLI 2.1.32 только с одинарным дефисом.
