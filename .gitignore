# --- Python ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.tmp

# --- Local configuration (may contain machine-specific absolute paths) ---
opencode.json
opencode.jsonc
serpent2-mcp.toml
.serpent2-mcp.toml

# --- Local project notes / handoff (not for the repository) ---
PROJECT_NOTES.md

# --- Local MCP runtime state (jobs, logs, caches) ---
.serpent2-mcp/
*.log

# --- Offline installation artifacts (see tools/make_offline_bundle.sh) ---
dist-offline/
wheelhouse/
*.tar.gz

# --- Serpent nuclear data libraries (large/proprietary; use tools/setup-env.sh) ---
*.xsdata
*.xsdir
*.dec
*.nfy
*.bra
data/
xsdata/
acedata/
photon_data/
mcplib63
mcplib84
sss2

# --- Photon data: mcplib84 is LANL/RSICC data, not for public redistribution ---
photon_libraries/
mcplib84

# --- Serpent calculations run locally (inputs/outputs) ---
In_*.txt
*.bumat*
*.dep
*.depmtx*
*.wrk
*.seed
*_res.m
*_det*.m
*_dep.m
*_bup.m
*_his*.m
*_mdx*.m
*_stat*.m
*_arr*.m
*_fmtx*.m
*_gsrc.m
*_nsrc.m
*.coe
*.out
*.png

# --- OS / editors ---
.DS_Store
.idea/
.vscode/

# Runtime artifact written by the data downloader when no explicit path is set
progress.json

# --- Frozen legacy MCP prototype (local only, never publish) ---
BACKUP/

# --- Personal task files (never commit) ---
*.docx
